Add property-level review UI with rating + TTL caching and pull-to-refresh across screens

- Add 'Add comment' flow on property details (star rating + comment sheet)
- Move comment UI out of booking and room-details container
- Add TTL-aware cache checks in `SupabaseProvider.fetchData` (default 1 hour)
- Add pull-to-refresh (`RefreshIndicator`) on Home, Properties, Room and Bookings lists
- Bump app version to 1.0.4+1
- Minor fixes and analyzer cleanups

Notes:
- Reviews are created via `RoomApi.createReview()` and refreshes reviews listing.
- The script `scripts/push_to_main.sh` stages, commits, pushes to `main` and deletes remote `dev` branch. Run it locally.
